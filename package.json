{
  "name": "decoration-tool",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "GITHUB_PAGES=true vite build && cp dist/index.html dist/404.html",
    "sync:docs": "rm -rf docs && mkdir -p docs && cp -r dist/* docs/ && cp dist/index.html docs/404.html && touch docs/.nojekyll",
    "build:pages:full": "npm run build:pages && npm run sync:docs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@element-plus/icons-vue": "^2.3.2",
    "@supabase/supabase-js": "^2.108.2",
    "element-plus": "^2.14.2",
    "vue": "^3.5.34",
    "vue-router": "^5.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.6",
    "vite": "^8.0.12"
  }
}
